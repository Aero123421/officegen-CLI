#!/usr/bin/env node
console.error("npm publish is intentionally blocked for this project.");
console.error("Use the checked GitHub Release tarball instead; the public npm package name is owned separately.");
process.exit(1);
