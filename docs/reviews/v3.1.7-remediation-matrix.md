# officegen v3.1.7 remediation matrix

Status values: `fixed`, `limited-disclosed`, `test-covered`, `deferred-v3`, `not-applicable`.

| ID | Area | Finding | Status | Evidence |
| --- | --- | --- | --- | --- |
| V31-P000 | Phase 0 P0 | v3.1.x publish readiness remains anchored to the perfect-spec evidence gate | fixed | `npm run perfect-spec:check -- --gate=publish` remains part of the release gate. |
| V31-R001 | Strict JSON | DOCX/XLSX visual verify could emit parser/font diagnostics before JSON | fixed | `packages/cli/src/shared/diagnostics.ts` and command registration keep JSON stdout redirected through payload and envelope emission. |
| V31-R002 | Strict JSON | Deferred stdout diagnostics could leak after payload completion | fixed | The diagnostics redirect waits one event-loop tick before restoring stdout. |
| V31-R003 | Strict JSON | Top-level `--help` and `--version` ignored JSON/strict-json mode | fixed | `packages/cli/src/program.ts` now returns JSON envelopes for top-level help/version under JSON mode. |
| V31-R004 | Agent Guidance | Agent-facing next commands were inconsistent about strict JSON | fixed | `packages/cli/src/shared/context.ts` and capability/help guidance now prefer `--agent --strict-json`. |
| V31-R005 | Error Catalog | `errors inspect TEXT_OVERFLOW` looked like a failed command | fixed | `packages/cli/src/shared/envelope.ts` treats successful error-catalog lookups as objective success. |
| V31-R006 | Inspect Narrowing | `inspect --slides` was recommended before being supported | fixed | `packages/cli/src/shared/metadata.ts`, payload plumbing, and PPTX inspect support `--slides <range>`. |
| V31-R007 | Inspect Narrowing | PPTX budget narrowing should recommend a working slide-range command | test-covered | `packages/cli/test/program.test.ts` covers `inspect --slides 1-5` and the recommended command string. |
| V31-D001 | DOCX/XLSX Visual | DOCX/XLSX `verify --visual` produced blank previews and blocked readiness | fixed | `packages/formats/src/view.ts` adds internal DOCX/XLSX object-map raster previews for non-native visual checks. |
| V31-D002 | DOCX/XLSX Visual | Internal DOCX/XLSX preview is approximate rather than native Office fidelity | limited-disclosed | View caveats identify the internal renderer and native proof remains separate. |
| V31-D003 | DOCX/XLSX Visual | Visual verify should not regress to blank artifact false failures | test-covered | `packages/formats/tests/formats.test.ts` covers DOCX/XLSX nonblank raster and visual verify usability. |
| V31-X001 | XLSX Formula | `xlsx.setFormula` could fail despite inspect seeing the target formula cell | fixed | `packages/formats/src/edit.ts` supports selector-based formula edits and workbook relationship targets. |
| V31-X002 | XLSX Formula | Selector fallback must not bypass failed selector guards | fixed | `xlsx.setFormula` now fails when the selector does not match, instead of writing by `selector.cell` alone. |
| V31-X003 | XLSX Formula | Schema should allow selector-only `xlsx.setFormula` operations | test-covered | `packages/core/src/schemas.test.ts` covers selector-based formula operation validation. |
| V31-F001 | Diff Parts | `changedParts` stayed zero for text/layout/DOCX/XLSX semantic changes | fixed | `packages/formats/src/diff.ts` now hashes semantic OOXML content parts such as slide XML, document XML, and worksheet XML. |
| V31-F002 | Diff Parts | Package part hashing could create false positives from volatile metadata | fixed | `packagePartHashes` filters to semantic Office parts and ignores `docProps` metadata churn. |
| V31-F003 | Diff Parts | Geometry and semantic part diff behavior needs regression coverage | test-covered | `packages/formats/tests/formats.test.ts` covers PPTX geometry, DOCX document XML, XLSX worksheet XML, and metadata-only no-op diffs. |
| V31-Q001 | PPTX Quality | Inspect did not elevate existing PPTX font/paragraph semantics | fixed | `packages/formats/src/inspect.ts` adds PPTX `styleInventory` and `layoutIssues` derived from semantic runs and bounds. |
| V31-Q002 | PPTX Quality | Object graph did not carry style semantics for agents | fixed | `packages/formats/src/graphs/objectGraph.ts` promotes dominant fonts, font size range, bullets, numbering, and italic/bold signals into node style. |
| V31-Q003 | PPTX Quality | Critique/improve missed fonts, bullets, line breaks, density, and overflow hints | fixed | `packages/cli/src/commands/payloads.ts` consumes PPTX semantic stats, layout issues, diagnose output, and style inventory. |
| V31-Q004 | PPTX Quality | Text overflow diagnosis was only length-based | fixed | `packages/formats/src/diagnose.ts` estimates text height from bounds, font size, and paragraph count for non-PDF Office objects. |
| V31-Q005 | PDF Quality | PDF text boxes should not fail readiness because of Office-style overflow estimates | test-covered | PDF overflow estimation is excluded from bbox-based checks; existing PDF verify/view tests pass. |
| V31-Q006 | Scaffold UX | PPTX scaffold produced a bare heading/paragraph seed | fixed | `scaffold` now emits a richer PPTX starter section with heading, list, table, and callout blocks. |
| V31-N001 | Native Verify | Native renderer errors mentioned LibreOffice even when Office COM was relevant | fixed | `packages/formats/src/export.ts` now describes Office COM and LibreOffice policy together. |
| V31-S001 | Package Risk | `ZIP_EMBEDDED_OBJECT` appeared in package gates but not top risks/issues | fixed | `packages/formats/src/verify.ts` promotes zip-safety caveats into warning summary, top risks, and report issues. |
| V31-S002 | Schema | Some high-level result schemas remain intentionally loose | deferred-v3 | Full inspect/verify/critique schema hardening remains future compatibility work; v3.1.7 adds focused tests instead. |
| V31-A001 | Version Sync | Version surfaces must be updated by script only | fixed | `npm run version:bump -- patch` synchronized manifests, lockfile, constants, and README release URLs to 3.1.7. |
| V31-A002 | Packaging | Release dist output must match source changes | fixed | `npm run build` regenerated package dist files after source changes. |
| V31-A003 | Packaging | npm distribution remains intentionally blocked | test-covered | `prepublishOnly` still points to `scripts/block-npm-publish.mjs`; GitHub Release tarball is the supported channel. |
| V31-A004 | Benchmark | Benchmark corpus behavior is unchanged by this patch | not-applicable | No benchmark manifests or benchmark command implementation paths were changed. |

## Release Gates

Required before v3.1.7 tagging:

- `npm run version:check`
- `npm run typecheck`
- `npm test`
- `npm run build`
- `npm run pack:smoke`
- `npm run remediation:check`
- `npm run contract:check`
- `npm run schema:coverage`
- `npm audit --audit-level=moderate`
- `npm run security:fixtures`
- `npm run benchmark:local-fixtures`
- `npm run perfect-spec:evidence`
- `npm run perfect-spec:check -- --gate=publish`
- `npm run release-tarball:smoke -- dist/officegen-v3.1.7.tgz`

Post-tag release completion additionally requires fresh v3.1.7 GitHub install and release-tarball smoke evidence.

## Review 1: Agent Contract / False Success

Reviewed strict JSON redirection, deferred stdout warning capture, top-level help/version JSON behavior, successful error catalog lookups, working inspect narrowing recommendations, and verify/package risk reporting.

## Review 2: Install / Release / Packaging

Reviewed version synchronization, generated dist output, GitHub Release tarball packaging, npm publish blocking, and the required v3.1.7 remediation matrix gate.

## Review 3: Asset / Office Workflow

Reviewed DOCX/XLSX internal visual preview behavior, XLSX formula selector editing, semantic OOXML diff part coverage, native renderer policy messaging, and embedded-object package risk promotion.

## Review 4: UX / Critique / Traceability

Reviewed PPTX style inventory, layout issue surfacing, object graph style semantics, critique/improve quality findings, richer scaffold defaults, and traceability from the v3.1.6 user feedback to v3.1.7 changes.
